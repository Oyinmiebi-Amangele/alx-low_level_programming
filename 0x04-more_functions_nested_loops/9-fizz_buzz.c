#include <stdio.h>

/**
 * main - prints FIZZ for each multiples of 3 and BUZZ
 * for each multiples of 5 and FIZZBUZZ for both multiples
 * for 3 and 5.
 * Return: Always 0.
 */

int main(void)
{
	int n;

	n = 1;
	printf("%d", n);
	for (n = 2; n <= 100; n++)
	{
		if ((n % 3 == 0) && (n % 5 == 0))
		{
			printf(" FizzBuzz");
		}
		else if (n % 3 == 0)
		{
			printf(" Fizz");
		}
		else if (n % 5 == 0)
		{
			printf(" Buzz");
		}
		else
		{
			printf(" %d", n);
		}
	}
	printf("\n");
	return (0);
}
