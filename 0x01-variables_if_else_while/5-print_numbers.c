#include <stdio.h>

/**
 * main - program that prints all singlt digit numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 48; i <= 57; ++i)
	{
		printf("%d", i);
	}
	putchar("\n");

	return (0);
}

