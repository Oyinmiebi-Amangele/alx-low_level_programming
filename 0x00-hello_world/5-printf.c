#include <stdio.h>

/**
 * main - using printf to print out a string.
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
